# 导航策略实施与阶段验证

2026-09-13。按[设计计划](DYNAMIC_AVOIDANCE_AND_NARROW_PASSAGE_DESIGN.md)实施；阶段验证未全部通过前不开放下一阶段。

## 当前状态与基线

P0–P2 已通过仿真检查；P3 开始实施，P4、P5 尚未放行。每阶段仿真验证通过后即可进入下一阶段，真机证据独立保留，不阻塞仿真开发。

- 原有 109 个控制源文件、参数和场景归档在 `/home/yjh/WorkSpace/astribot_validation/policy_p0_20260913/`，含 SHA256 清单及两轮 12/12 到点证据。该归档包含当时仓库的未提交改动。
- 原基线耐久测试为 108/108 到点，已归档至 `/home/yjh/WorkSpace/astribot_validation/endurance_quality_20260913/`；不能将这些结果套用于新增策略。
- 默认 `navigation_policy_stage=off` 保留原跟踪链路；显式 `p2` 才接入新策略。ThreePhase、MPPI 和到点控制的核心公式与已接受参数保持不变。
- 新包的领域契约覆盖 SI 单位、时间/epoch、视觉几何/协方差/来源、健康信息、执行与规划意图、不可变快照及版本权限。51 项外部契约检查通过。
- 最新阶段回执在 `/home/yjh/WorkSpace/astribot_validation/policy_sim_stages/`。早期 P0 回执中的真机阻塞状态已由这里的仿真阶段回执替代。
- 验证脚本和原始记录留在外部验证目录，不安装进产品包。

## 真机验证接口与证据要求

设计计划 P0 要求“补齐真实尺寸、载荷、制动/延迟、盲区测量”。目前未获得下列可追溯实测资料，不能从仿真配置或注释推定为已验证：

| 实测项 | 需要的证据 |
|---|---|
| 运输姿态全身包络 | 关节姿态、对应全身尺寸/轮廓、坐标系、时间和测量误差；包括夹爪 |
| 载荷 | 实际携带物外形、质量、固定方式和可能摆动范围；空载也需明确工况 |
| 最差停止包络 | 指定姿态/载荷、地面和初速度下，各允许运动方向的实测停止距离与角余转 |
| 端到端延迟 | 感知采集/推理/传输、决策和执行器响应的时间记录，以及时钟对齐方法 |
| 近场和遮挡覆盖 | 雷达/深度/视觉有效视野、近场盲区、自滤范围和机械臂/载荷遮挡；所需标定版本 |

按用户最新要求，P0 和后续阶段先采用现有仿真文件参数与明确标注的保守假设。每阶段仿真验证通过即可进入下一阶段，真机实测不再阻塞仿真开发。上述真机证据仍需在硬件放行前补齐，不能把仿真结果当作真机标定结果。

仿真参数位于新包 `config/simulation.json`：0.62 m 方形运输包络、0.35 m/s 基线上限、0.5 m/s² 加速度；保守制动减速度 0.5 m/s²、响应时间 1.0 s（0.6 s 运动估计窗口、0.3 s 扫描预算及 0.1 s 适配预算，也覆盖底盘 0.5 s 命令看门狗） 均标为仿真假设。启动接口拒绝在非仿真时间下加载该仿真参数集。

最新阶段回执位于 `/home/yjh/WorkSpace/astribot_validation/policy_sim_stages/`，覆盖早期 P0 回执中的实测阻塞状态。

## P1：仿真观测验证通过

已接入 Gazebo 的扫描、里程计、地图和 TF，新增时间戳明确的视觉 JSON 适配接口；不发布运动命令。实现目标关联、相关来源去重、预测包络、真实运动方向扫掠检查、遮挡占据保留和显式自由空间解除。

51 项契约检查、20 项观测回放和 11 项实时接口检查通过。实时检查涵盖实际扫描输入、视觉米制框、无深度未知、迟到/负尺寸拒绝、过期不自动清空以及显式来源解除。日志及回执为 `contracts.log`、`p1_replay.log`、`p1_current_live.json`、`p1.json`。

P2 已通过当前配置的仿真回归；P3–P5 仍须按顺序完成仿真验证。视觉输入为模拟消息，不能声称完成真机多传感器标定验证。

默认 `off` 时保持现有静态跟踪链路；新增控制节点仅在显式启用 P2 时接入。不能用 `check_executable` 通过来宣称路径无碰撞，它只检查权限和契约，后续还必须接入几何、时空风险和接管连续性验证。

## P2：0.32 m/s 仿真配置通过

验证入口：

```bash
bash tools/launch_sim_stack.sh --mode baseline --navigation-policy p2 --max-linear-speed .32
```

原 `off` 基线及其 0.35 m/s 一键启动默认值保留。P2 的验收工况明确为上面的 0.32 m/s；不能把本次结果套用于其它速度、姿态或载荷。回执为外部验证目录的 `p2.json`，源码快照为 `speed032_source_manifest.json` / `speed032_source.tar.gz`。

| 验证 | 结果 |
|---|---|
| 相同初始化的返回段，两轮对照 | 4/4 到点，零策略停车；角加速度 P95 均值/最大值 0.181453/0.181927 rad/s²，原基线 0.186332/0.195075 |
| 普通六点路线 | 6/6 到点，最大 2.294 cm / 1.002°，零策略停车；横向、航向、加减速和实测曲率/曲率变化率均未超过原 18 轮逐目标上界 |
| 持续让行 | 同一 action 暂停 24 s 后恢复；零速、停稳后零漂移；停车 0.02502 m ≤ 对应初速度预算 0.19330 m；到点 1.618 cm / 0.782° |
| 实体挡路后移除 | 到点 2.216 cm / 0.926°；保守最小间隙 0.70327 m；无额外重规划 |
| 实体横穿与模拟视觉速度 | 预测冲突、让行并恢复；到点 1.582 cm / 0.890°；最小间隙 0.54289 m；无额外重规划 |
| 独立故障注入 | 7 项通过，覆盖扫描/策略/命令中断、异常量程和恢复；输出不连接机器人命令话题 |

持续时间只记录，不作为跟踪质量评分。停车距离按初速度对应制动预算验收，不能把 3 cm 到点误差当成紧急停车距离要求。真机结果仍未验证。

### 实现与回归范围

- 统一减速、让行、清空确认；末级原始扫描防护、墙钟看门狗、恢复加速度限制；Arrival、ThreePhase 和进展检查器识别合法暂停。
- 行为树将路径碰撞证据提交统一策略，避免碰撞检查先于 HOLD 到达时抢先重规划。默认关闭策略时保留原重规划行为；P2 尚不启用绕行。
- 扫描等待采集时刻 TF，最多保留 5 帧，仍按原采集时间过期；融合采用一致快照。空间关联索引保留原匹配判据，265 组状态比较一致；密集区实测处理耗时最大约 89 ms。
- Nav2 专用扫描适配仅转换无回波清除端点，并要求端点超过障碍标记范围；原扫描继续独立防护。3 项 LaserProjection 检查通过。
- 浮点模长舍入不再错误开启限制器恢复；3 项定向检查通过。角运动、命令超时与约束有效期移入仿真/硬件模板，值不变；10,000 组命令和 1,000 组扫掠与归档实现完全一致。
- 14 项策略回放、6 项 TF 队列检查、3 项快照检查及 84 项 C++ 回归通过。沿用的融合、风险和策略模块源码对应关系保存在 `speed032_carried_evidence_sources.json`。

### 启动和评测修复

低速启动从 YAML 读取转弯最低速度，并约束为不大于请求上限；0.35 m/s 时仍为原值 0.324。4 项参数展开检查及实际 0.32 m/s 生命周期启动通过。

管理器按本次会话父子关系和启动时间回收脱离进程组的子进程，使用 pidfd 防止 PID 复用；4 项进程隔离检查通过。此项不表示已解决 Gazebo 插件加载偶发停滞的根因。

评测使用里程计采集时间计算运动导数，在相位/路径变化处断开差分；保留旧轮询时间字段。等待 action 发现期间持续处理消息，发现稳定后重新获取新鲜位姿，保存实际控制器参数；各 4 项时间口径和等待检查通过。

早期 0.35/0.34 m/s 失败试验、输入停顿和定位过程保存在外部目录的原始记录与 `p2_implementation_history.md`。旧客户端清理不作为停顿根因已解决的证据；浮点修复也不作为全部质量差异的因果解释。

## P3：实施中

P2 仿真通过后开放本阶段；完成局部/全局候选生成、联合决策、完整候选检查、目标绑定的原子提交和有界不可达结果后，再进行闭环仿真。P4/P5 保持未开放。阶段回执为 `p3.json`。

已实现 `planning_session.py`：局部/全局候选共享同一有效请求，绑定目标、路径、地图及包络版本，拒绝迟到结果；每目标最多 5 次请求、单请求 2 s、阻塞 episode 30 s（稳态时钟）。前两项沿用已有路径守卫边界，30 s 为仿真起始假设；参数已同步到真机模板。16 项隔离检查通过。该模块只管理请求，不授予运动权限。

`/path_tracking/plan_candidate` 使用 `astribot_navigation_msgs/srv/PlanCandidate`，支持前方接回、全局候选和候选几何复核。它复用原规划及曲率处理，所有候选再做完整配置足迹扫掠；不会把候选发布成 `/plan`。`geometry_valid` 只表示本次二维几何检查通过，不代表时空交互、动态包络或接管已获准。15 项 Gazebo 接口检查与 84 项原 C++ 回归通过。

仍待接入：统一让行/局部/全局决策、时空与包络复核、目标上下文和原子提交、规划并发/取消，以及将有界失败反馈到导航 action。当前仍以 P2 运行，尚未开放动态绕行或 P4。

新增几何接口后，原 P2 控制链往返烟雾检查 2/2 到点通过，记录为 `p3_geometry_baseline_check.log`；这不替代 P3 的闭环阶段验证。
