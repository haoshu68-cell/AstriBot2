# 2026-09-14 底盘跑机复盘与脚本修复

数据目录：`runs/x_positive_20260914_162319`。本次复盘只读取已有记录，在本机使用模拟 SDK 验证，没有发送真机运动指令。修复范围为测试脚本、配置模板及说明，不涉及导航和路径跟踪控制器。

## 原因与证据

| 问题 | 原始证据 | 修复 |
|---|---|---|
| 累计转角保护被微小往返变化触发 | 62497 帧覆盖约 250 秒；原始累计平移 1.38318 m，小于 2 m；累计绝对转角 0.99999689 rad 已紧贴 1 rad 上限，而全程航向范围仅 0.4417°。下一帧触发异常但未写入旧 CSV | 从累计锚点计算达到分辨率的变化；保留原始总变化供诊断；连续慢移、慢转及锚点剩余变化仍参与预算。新异常写明测量值与限值，触发帧保存在 CSV |
| 所有厂家最小速度候选被否决 | 基线 0.0602 秒时 vx 峰值 0.0151956 m/s，乘 5 后门槛 0.07598 m/s，大于最高测试档 0.02 m/s；第 1 秒后厂家 vx 为零 | 剔除可配置启动瞬态，以源时间加权的 1 秒均值估计基线速度噪声，保留分段持续位移和重复性要求；明显漂移的基线不输出候选 |
| SDK 退出被过早判阻塞 | 停车请求返回后 0.10058 秒出现 `SDK blocked`；旧逻辑遇 EOF 提前跳出 5 秒循环，再只等待 0.1 秒 | EOF 仅表示管道关闭，继续等待进程到实际退出或完整截止时间；等待期间持续消费反馈；真实退出超时单独记录 |
| 停车缺证据 | 原 `stop_feedback.jsonl` 为空，厂家反馈最终报源数据间断。无法凭记录确认当时物理静止，也不能确定间断完全由脚本还是厂家退出引起 | 等待期间持续采集，仅停车回执之后的新源数据用于确认；不清除锁存的数据异常，不把请求返回当作静止 |
| 低速停车时间近零 | 旧静止阈值 0.01 m/s 高于多个测试档位 | 试验轴阈值不高于最低档的 1/4，同时检查速度绝对值的时间均值和位姿范围；未检出初始运动或未确认静止时不给停车时间/确认距离 |
| 离线重算可能掩盖异常 | 原运行未完成，SLAM 嵌套候选仍存在；旧 `--analyze` 不读取执行状态 | 保留原执行失败状态，主源和参考源正式候选置空；离线候选单独标注 |

厂家反馈与 SLAM 均不是本次独立地面真值。SLAM 的同时间戳同位姿重复数据已正确去重，不能将参考检出门槛当作底盘死区。

## 原数据重算

[厂家重算结果](../runs/analysis_x_positive_20260914_162319/summary.json)与 [SLAM 重算结果](../runs/analysis_x_positive_20260914_162319/slam_reference/summary.json)保存在新目录，原文件未覆盖。

- 厂家有效累计平移：1.24637 m；有效累计转角：0.10902 rad。已有数据重放未触发原 2 m / 1 rad 上限。这不证明缺失的最后约 10 秒一定能够通过。
- 厂家 0.002 m/s 三次恒速段通过位移、方向和速度判定，形成离线候选；SLAM 离线候选仍为 0.006 m/s。两者不能替代外部实际位移测量。
- 两份结果均保留原 `stop_unconfirmed`，正式 `minimum_continuous_motion_candidate` 均为 `null`，`hardware_validated=false`。
- 厂家 0.002 m/s 三次计划停车阈值延迟重算约为 0.672、0.944、0.905 秒，代替旧的接近零结果。这包含正常减速/filter 过程，不能用作机械急停制动时间。

## 验证

验证脚本保存在仓库外 `/home/yjh/WorkSpace/astribot_validation/chassis_characterization`，避免将临时单元测试混入产品仓库。

- 19 项既有计划、方向、持续运动和模拟 SDK 检查通过。
- 12 项反馈去重、冲突、时钟回退、数据间断和来源约束检查通过。
- 11 项针对本次问题的回归检查通过，包括慢速越界仍触发、航向跨 ±π、正反向低速不判停车、零净位移振荡不判停车、EOF 后延迟退出及真实超时。
- 8 项终端停车、STOP 文件和信号处理检查通过。
- 本机 ROS Domain 198、仅本机通信、替换为模拟 SDK：正常结束、反馈中断、STOP 文件中止均符合预期。
- 额外按 4 ms 周期配置合成发布，收到 2448 帧厂家反馈，正常完成并确认静止，未出现源数据异常。验证不加载厂家 SDK，不连接真机控制接口。
- SDK 导入失败与构造阶段失败区分检查通过。

仍需现场验证新版的完整运行及停车反馈连续性。默认静止阈值更严格，若无法确认静止，应保存原始反馈查明噪声、爬行或数据中断原因，不应为让测试通过而直接放宽保护。首次修复已同步机器人；后续实测迭代见 [X 正向低速复测](CHASSIS_X_MIN_SPEED_20260914.md)。
