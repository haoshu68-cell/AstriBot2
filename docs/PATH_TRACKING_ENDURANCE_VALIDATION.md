# 本轮验证记录（2026-09-13）

本轮证据目录：`/tmp/astribot_event_tracking`。操作说明见 [PATH_TRACKING_ENDURANCE.md](PATH_TRACKING_ENDURANCE.md)。以下区分已通过的验证和仍在运行的耐久验证。

## 构建与兼容性

- path_tracking、navigation 编译通过；5 个临时回归程序内共 81 项检查通过：原对齐数学 54 项、到位控制 19 项、规划兼容性 1 项、新增路径质量 6 项、事件触发状态机 1 项。临时检查未加入仓库。
- 路径质量检查覆盖直线、尖角、位移边界、无效输入、急弯速度限制，以及栅格端点噪声不会误触发降速。
- 状态机检查覆盖首次目标、目标变化、路径有效时不规划、明确碰撞后规划、服务不可用与碰撞的区别。
- ThreePhaseController、align_math、原 MPPI 曲率评分及数学实现与本轮开始时的归档逐字节一致。正常路径的内层规划结果保持兼容；新增的急弯降速只作用于超限窗口。
- Python 语法、XML/YAML 解析、事件行为树及插件注册检查通过；指标的几何投影、角度归一化、无样本 null 检查通过。

## 启动

分阶段启动已实测成功；扫描源同时传给感知、两个代价地图，并读取运行时参数确认。

测试中也重现了一次 DDS 生命周期响应丢失：仿真 120 秒内时钟、odom、扫描持续正常，而 Nav2 未激活。启动器保留失败证据，只重启本次创建的导航进程组，第二次激活成功。日志在 `stack4/navigation_1.log`、`stack4/navigation_2.log` 与 `supervisor4.log`。这验证了有限重试和故障识别，不能据此声称 DDS 根因已解决。

最初拆分启动时发现漏传 scan_topic，已修正并增加实际参数核验。修正前的扫描注入试验不计为碰撞验证通过。手工单束扫描注入也未证明代价地图产生障碍，后续改用真实 Gazebo 模型与传感器链路验证。

## 碰撞事件

Gazebo `/world/default/create` 创建 0.6 × 0.6 × 0.9 m 的障碍箱，位于当前路径前方 (0, -3)。在已有导航目标开始后加入，通过真实传感器进入代价地图。

- 一个目标变化事件、一个 collision 事件；绕行目标对应两条路径，返回目标对应一条路径。
- 日志显示 collision 后旧 FollowPath 被取消并停止机器人，随后执行新路径。
- 绕行到点：2.295 cm / 0.253°，action succeeded。
- 返回到点：2.106 cm / 0.992°，action succeeded。
- Gazebo 障碍创建及删除服务均返回 true。没有留下测试障碍。
- 绕行过程有横向 P95 超过 10 cm 的观察告警；到点成功不代表绕行过程无偏差。

证据：`gazebo_obstacle.json`、`collision_physical/results.jsonl`、`stack5/navigation_1.log`。

## 多目标与短程回归

两个共线中间目标的 NavigateThroughPoses 成功，最终 1.626 cm / 0.906°，只发生一次初始规划。中间点移除不触发新规划。测试还暴露了栅格端点拼接造成的误降速，已加入仅用于指标的噪声过滤，并补充回归检查。

旧版相同五点短程路线：总耗时 108.55 s，最大位置误差约 1.88 cm、最大角度误差约 1.00°。旧日志中五个目标初始规划之外，还有 92 次 “Passing new path to controller” 更新。旧证据：`/tmp/astribot_logic_fixes/nav_delivery.log` 与 `navigation_delivery.json`。

最终版五点全部通过，总耗时 106.67 s，最大位置误差 1.937 cm，最大角度误差 1.024°。每点一条规划路径，无碰撞重规划、无过程告警。单轮统计受 MPPI 随机采样影响，不证明所有场景均改善。

| 目标 | 位置误差/cm | 角度误差/° | 耗时/s | 路径数 |
|---|---:|---:|---:|---:|
| 前进 | 1.754 | 0.750 | 15.46 | 1 |
| 侧移 | 1.425 | 0.978 | 22.76 | 1 |
| 斜行 | 1.937 | 1.024 | 20.26 | 1 |
| 原地调角 | 1.740 | 0.956 | 27.46 | 1 |
| 返回 | 0.699 | 1.003 | 20.71 | 1 |

证据：`short_final/results.jsonl`、`short_final/samples.csv`、`endurance_stack/navigation_1.log`。

## 六点循环耐久

默认参考路线为 (0,-8.3)、(0,0)、(-5.31,9.25)、(5.45,1.90)、(-2.8,-8)、(0,0)，各点保留参考脚本 yaw。预检查已验证六段可规划；两个超限候选的曲率变化率经平滑分别由 21.13→1.82、12.58→5.22 m⁻²，碰撞复检通过。来源为 `preflight_fixed` 与 `stack4/navigation_2.log`；正式循环开始时会再次按最终配置预检查。

两小时耐久未结束前，只能报告阶段结果。需同时审阅到点误差、横向/航向偏差、旋转/停滞、加速度/jerk、路径曲率与事件次数，不应把 action succeeded 当作全过程合格。未宣称 SLAM、视觉或 mark 的真机精度已由本次 ground_truth 仿真验证。

## 首次长路线运行及发现的问题

运行器 PID 1107212，Gazebo/Nav2 启动会话在 `/tmp/astribot_event_tracking/endurance_stack`；运行记录在 `/tmp/astribot_event_tracking/endurance`。2026-09-13 18:14:52 开始计时，计划 20:14:52 结束（北京时间）。该次运行在第五点异常后自动停止，未达到两小时。监控已暂停，待修正后的运行启动后恢复。

首轮前三个目标均到位合格，但过程指标发现差异：

- 8.3 m 去程直线横向 P95 4.59 cm，正面方向 P95 11.71°。
- 直线返回横向 P95 4.01 cm；大航向偏差仅发生在距目标 0.30–0.37 m，处于现有 GoalAngleCritic 的 0.5 m 预对齐区域，应与无谓旋转区别。
- 第三段弯道横向 P95 46.39 cm、最大约 54.92 cm，轨迹显示切角；部分大朝向偏差发生在距目标 0.38–1.26 m，并非都能解释为末端预对齐。因此不能把该段的到位成功描述为全过程跟踪优良。该问题已记录为过程告警，保留原核心评分参数继续观察，尚未证明全路线跟踪效果优于旧版。

轨迹核对图：`/tmp/astribot_event_tracking/first_goals.png`。图中的参考线由已记录的投影、横向误差和路径方向重建，仅用于诊断，不替代原始规划路径。


第五点在离目标 29.03 cm 时出现 REFINEMENT_BLOCKED。读取当时局部地图：轮廓边界最大代价 253、中心 108，没有致命栅格。原精调把内切膨胀区再次施加在完整轮廓边界，导致误阻塞。现已与全局复检统一为边界拒绝 ≥254、中心仍拒绝 ≥253；另有两个回归案例验证边界的致命障碍及内切区域中的中心仍会被拒绝。

修正后的原目标 (-2.8,-8,π) 复测成功：1.093 cm / 1.046°；返回原点 1.753 cm / 0.976°。到位阈值未放宽。证据：`local_footprint_before.json`、`refinement_fixed/results.jsonl`。弯道偏离过程告警仍保留，未通过修改阈值掩盖。

另外，平滑输出已恢复原路径点数，避免 MPPI 的 offset_from_furthest 等按点数参数因路径采样减半而扩大实际前视距离；新增了对应回归检查。


## 修正后的正式循环（运行中）

- 最终构建及 81 项回归检查通过（`build_delivery.log`、`regression_result_delivery.log`）。原控制器和规划器 YAML 参数与本轮起始归档一致。
- Gazebo/Nav2 会话：`/tmp/astribot_event_tracking/final_stack`；运行器 PID 1208598，记录目录 `/tmp/astribot_event_tracking/endurance_verified`。
- 2026-09-13 18:33:09 开始两小时计时，计划 20:33:09 结束，实际截止时间以 status.json 为准。监控已恢复，读取新目录。
- 本轮新增保存每个目标的起止时间、原始规划路径、目标/循环编号、参数及脚本快照；末端预对齐和行进航向指标分别记录。

首轮六点全部到位合格，用时 342.85 s，最大欧氏误差 2.737 cm、最大角度误差 1.387°。每目标只产生一次规划，未发生无碰撞原因的周期重规划。第五点误阻塞未再出现。当前进入第二轮，尚未跑满两小时。

| 点 | 位置误差/cm | 角度误差/° | 横向 P95/cm | 行进朝向 P95/° | 路径数 |
|---|---:|---:|---:|---:|---:|
| 1 | 2.737 | 0.280 | 19.66 | 21.78 | 1 |
| 2 | 1.471 | 0.991 | 1.84 | 4.32 | 1 |
| 3 | 2.161 | 1.387 | 9.47 | 21.83 | 1 |
| 4 | 1.105 | 0.952 | 20.11 | 16.11 | 1 |
| 5 | 1.986 | 0.499 | 31.28 | 27.14 | 1 |
| 6 | 1.869 | 0.530 | 9.73 | 14.27 | 1 |

第 1、4、5 段仍有横向 P95 >10 cm 的观察告警，最高 31.28 cm；未触发持续 FOLLOW 旋转告警。它们仍需在后续循环中观察，不能宣称全程贴线精度达到 3 cm，也不能宣称所有工况优于旧版。

轨迹图：`/tmp/astribot_event_tracking/verified_first_cycle.png`；这是根据当前原始 plans.jsonl 和 samples.csv 绘制的实际路径对照。



## 自动监控阶段记录：18:52

修正版已完成 3 个完整循环及第四轮前三点，共 21/21 个目标到位合格。最大欧氏误差 2.975 cm、角度误差 1.464°，无测量缺帧；每目标仍只规划一次，无碰撞重规划。运行器状态 running，状态文件持续更新。

第六段横向 P95 连续三轮为 9.73、14.36、24.90 cm，出现连续两轮增加，已作为新的恶化趋势通知。其他弯道切角告警存在波动，未见持续 FOLLOW 旋转或停滞告警；未调整参数、重发目标或重启仿真。

每轮汇总见 `/tmp/astribot_event_tracking/endurance_verified/monitor_checkpoint.json`，继续运行至既定截止时间，尚未完成两小时验证。

## 航向优化前耐久归档

因开始航向收敛优化对照实验，主动取消当前目标并结束本轮耐久。状态 `stopped`，实际持续 52.35 分钟，完成 55 个目标、9 个完整循环；完成目标 55/55 到位合格，最大位置误差 2.975 cm、航向误差 1.478°。当前被取消目标不计为成功。没有跑满两小时，不能作为两小时通过结果；原监控已暂停。原始数据保留于 `/tmp/astribot_event_tracking/endurance_verified/`。


## 质量优先调优后的复验与耐久启动

2026-09-13：同路线两轮 12/12 到位通过，最大 2.128 cm / 1.126°，无 FOLLOW 异常旋转或无效采样。仿真速度 0.35 m/s，跟踪总时长不参与性能评价。直线/弯道/弯后窗口误差及未改善项见 `HEADING_CONVERGENCE_VALIDATION.md`；第二段横向 P95 和部分角加速度仍高于起步修复基线，不能宣称全部指标均改善。

已接续启动两小时耐久：`/tmp/astribot_heading/endurance_quality/`；栈为 `/tmp/astribot_heading/native_quality_stack/`，监控已改为该目录。此处仅记录启动，不代表两小时完成。到期取消的未完成目标单列。


## 质量优先两小时耐久最终结果（2026-09-13）

运行覆盖 20:36:38–22:36:38（北京时间），状态 `duration_complete`。完整覆盖两小时；时长仅表示耐久覆盖，不参与性能评分。18 个完整六点循环，108/108 个目标通过 action、3 cm/1.5° 和停稳校验，最大到位误差 **2.926 cm / 1.484°**。第 19 轮第 1 点（第 109 个目标）到期取消，`cancel_action_status=5`，没有 cancel_error，不计入到位成功数。

无无效采样。已完成目标均只因 new_goal 规划一次，共 108 次；候选质量记录 accepted 77 次、smoothed 31 次。没有碰撞重规划或异常旋转告警；低速旋转采样累计 0.099 s，最长 FOLLOW 低速停滞 0.403 s。15 个目标出现横向 P95 超过 10 cm 的过程告警，仍需保留，不能因到位成功忽略。

对照为前期 `native_quality/` 两轮。以下均排除到期取消的未完成目标，使用相同窗口规则，数值为“原两轮→本次耐久”：

| 窗口 | 航向 RMS/P95 ° | 横向 RMS/P95 cm | 加速度 P95 m/s² | 角加速度 P95 rad/s² | jerk P95 m/s³ |
|---|---|---|---|---|---|
| 直线 | 1.98/4.12→1.59/3.27 | 2.57/4.56→2.31/4.70 | 0.083→0.091 | 0.104→0.106 | 0.617→0.707 |
| 弯道 | 5.42/12.74→5.46/12.63 | 4.35/9.02→4.31/8.86 | 0.067→0.074 | 0.235→0.241 | 0.542→0.584 |
| 弯后 1 m | 1.39/2.69→1.79/3.63 | 2.92/6.19→3.10/6.13 | 0.035→0.041 | 0.127→0.128 | 0.293→0.335 |
| 接近 | 3.39/7.85→2.50/6.09 | 4.35/10.06→3.74/8.82 | 0.080→0.079 | 0.178→0.188 | 0.610→0.618 |
| 精调 | 不适用 | 不适用 | 0.026→0.023 | 0.090→0.088 | 0.206→0.192 |

直线/弯道/弯后指标只统计末端朝向区之外的 FOLLOW；接近段速度统计范围 0.3–1.5 m，航向和横向统计仅使用其中距目标大于 0.5 m 的样本，避免把目标 yaw 预对齐当作路径航向误差。精调仅列速度导数。窗口可能重叠；加速度与 jerk 不跨目标、路径版本、阶段或数据断档求导。统计是按时间采样的合并分布，不等同于空间等权统计。实测轨迹曲率 P95 的各目标中位值为 0.971→0.977 m⁻¹。

到位与耐久覆盖通过；部分直线 jerk、弯后航向等指标高于前两轮，不能宣称所有过程质量均改善。原始数据保存在 `/tmp/astribot_heading/endurance_quality/`，完整汇总和分析脚本归档在 `/home/yjh/WorkSpace/astribot_validation/endurance_quality_20260913/`。本次结果不替代动态障碍、窄通道或真机标定验证；P0/P1 阶段状态不因耐久完成自动放行。监控在完成归档后暂停。
