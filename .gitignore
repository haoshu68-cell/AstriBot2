*build
__pycache__
.vscode
wandb/
install
log
.idea
astribot_sdk_develop_ws.code-workspace
CLAUDE.md
.claude
# 仿真跑出来的地图产出（.posegraph 单个就有 18MB，重新建图即可再生，不入库）
maps/

# 测试与诊断产物（本轮实机排障产生）
.coverage
frames_*.gv
frames_*.pdf

# 底盘基准套件的跑分产出（带时间戳、重跑即可再生）。
# 不入库的关键理由不是体积，而是：一旦入库，将来真机跑出的**真实测量数据**
# 会脏工作树，很容易与示范产物混着提交出去。
chassis_benchmark/results/
